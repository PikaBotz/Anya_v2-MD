const reactions = require(__dirname + '/reactionMedia');
const { createGoodbyeThumb } = require(__dirname + '/goodbyeTemplate');
const Template = require(__dirname + '/knights');
const { Questions, Quiz } = require(__dirname + '/quiz');

module.exports = {
    reactions,
    Template,
    createGoodbyeThumb,
    Questions,
    Quiz
};
