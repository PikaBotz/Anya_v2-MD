const { PinterestDownloader } = require(__dirname + '/pinterest');
const { playStoreSearch } = require(__dirname + '/playstore_search');
const { SoundCloudeSearch } = require(__dirname + '/SoundCloud_search');
const { RingTone } = require(__dirname + '/ringtone');

module.exports = {
  PinterestDownloader,
  playStoreSearch,
  SoundCloudeSearch,
  RingTone
};
