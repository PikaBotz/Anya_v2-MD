const { imageEditor } = require(__dirname + "/imageEditor");
const { remini } = require(__dirname + "/remini");
const { ttp } = require(__dirname + "/ttp");

module.exports = {
  imageEditor,
  remini,
  ttp
};
